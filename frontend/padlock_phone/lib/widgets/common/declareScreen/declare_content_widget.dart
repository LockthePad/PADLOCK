import 'package:flutter/material.dart';

class DeclareContentWidget extends StatelessWidget {
  const DeclareContentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('건의하기의 내용이 입력될 위젯입니다.'),
    );
  }
}
