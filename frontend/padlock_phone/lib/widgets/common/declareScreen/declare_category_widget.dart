import 'package:flutter/material.dart';

class DeclareCategoryWidget extends StatelessWidget {
  const DeclareCategoryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('건의페이지의 카테고리를 선택하는 위젯입니다.'),
    );
  }
}
