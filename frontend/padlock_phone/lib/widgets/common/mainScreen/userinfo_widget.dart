import 'package:flutter/material.dart';

class UserinfoWidget extends StatelessWidget {
  const UserinfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('유저 정보가 담긴 위젯입니다.'),
    );
  }
}
